name: execute-v1.3.1-request-id-closure

on:
  push:
    branches:
      - "exec/v1-3-1-request-id-closure-v1"

permissions:
  contents: read

concurrency:
  group: execute-v1.3.1-request-id-closure
  cancel-in-progress: true

jobs:
  superseded:
    runs-on: ubuntu-slim
    timeout-minutes: 10
    steps:
      - run: echo "Superseded before publication by exact-head Rust fmt/Clippy remediation."
